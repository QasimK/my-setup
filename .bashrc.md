# .bashrc additions

```
# Do not save duplicates and commands prefixed with a space
HISTCONTROL=ignoreboth

# Bash Completion
if [ -f /etc/bash_completion ] && ! shopt -oq posix; then
    . /etc/bash_completion
fi

# Paths
export PATH=~/.local/bin:${PATH}
```
